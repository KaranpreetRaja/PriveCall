<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <script>
            const ROOM_ID = "<%= roomID %>"
            const NICKNAME = "<%= nickname %>"
        </script>
        <script>
            const DisplayName = 'Karan'
        </script>

        <script defer src="https://unpkg.com/peerjs@1.2.0/dist/peerjs.min.js"></script>
        <script src="/socket.io/socket.io.js" defer></script>
        <script src="/public/script.js" defer></script>
        <title>Document</title>
        <style>
            #video-format {
                height:100%;
                width:70%;
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(505px, 1fr));
            }

            video {
                width: 100%;
                height: auto;
                object-fit: fill;
            }

            html {
                height: 100%;
                width: 100%;
            }

            body {
                height: 100%;
                width: 100%;
                margin: 0;
                font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
                overflow: hidden;
            }

            #messaging {
                position: fixed;
                height: 100%;
                width: 30%;
                display: grid;
                right: 0;
                top: 0;
            }

            #form {
                background: rgba(0, 0, 0, 0.15);
                padding: 0.25rem;
                position: fixed;
                bottom: 0;
                right: 0;
                display: flex;
                height: 3rem;
                width: 30%;
                box-sizing: border-box; backdrop-filter: blur(10px);
            }
            #input {
                border: none;
                padding: 0 1rem;
                flex-grow: 1;
                border-radius: 2rem;
                margin: 0.25rem;
            }
            #input:focus {
                outline: none;
            }
            #form > button {
                background: #333;
                border: none;
                padding: 0 1rem;
                margin: 0.25rem;
                border-radius: 3px;
                outline: none;
                color: #fff;
            }
            #messages {
                list-style-type: none;
                margin: 0;
                margin-bottom: 3rem;
                padding: 0;
                border: black solid 2px;
                border-bottom: none;
                overflow-y: scroll;
            }
            #messages > li {
                padding: 0.5rem 1rem;
            }
            #messages > li:nth-child(odd) {
                background: #efefef;
            }
            #typinginsight > li:nth-child(odd) {
                color: #aaa;
                padding: 14px 0px;
                margin: 0 20px;
            }

            p {
                position: fixed;
                margin-bottom: 3rem;
                right: 22%;
                bottom: 0;
            }
            #roomID {
                position: fixed;
                right: 70%;
                bottom: 0;
            }
            
        </style>
    </head>
    <body>
    
        <div id="video-format"></div>

        <div id="messaging">
          <ul id="messages"></ul>
          <div id="typinginsight"></div>
          <form id="form" action="" onsubmit="return false">
              <input id="input" autocomplete="off"/>
              <button id="send">Send</button>
          </form>
          
        </div>
        <h3 id="roomID">Room ID: </h3>
        <script>document.getElementById("roomID").innerHTML += ROOM_ID</script>
    </body>
</html>
